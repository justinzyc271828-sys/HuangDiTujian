# 工作区边界（硬规则）

## 唯一可写路径

**工作目录（Workspace Root）：**

```text
D:\Workspaces\Github\HuangDiTujian
```

Agent / 自动化助手 **只能** 在此目录及其子目录内：

- 创建文件与文件夹  
- 修改、删除、重命名  
- 运行以本仓库为产出目标的脚本（输出也须落在本目录内）

## 禁止

- 在工作区 **之外** 创建、修改、删除任何文件（包括但不限于 `D:\Reference\...`、用户主目录其它路径、系统目录）  
- 把「参考库大文件」默认写到盘符根下的其它树；若需要大资料，一律放在本仓库约定子目录（见下）  
- 未经用户在当次对话中 **明确书面授权** 的例外路径操作  

## 资料应放在哪里

| 类型 | 本仓库路径 |
|------|------------|
| 项目文档 | `docs/` |
| 史源卡 / 史料卡 | `content/sources/` |
| 产品数据 | `data/` |
| 画像与地图素材 | `assets/` |
| 可版本管理的参考摘录 | `docs/references/`、`content/sources/**/摘录/` |
| 体积较大的本地参考包 | `reference/`（本仓库内，可被 `.gitignore` 忽略） |

> 说明：早期曾在仓库外使用 `D:\Reference\HuangDiTujian-Ref`。自本规则生效起，**不再向该路径写入**；新材料只进本仓库 `reference/` 等约定目录。仓库外已有文件由用户自行管理，Agent 不主动改动。

## Git

- 大文件目录若不宜入库，在根目录 `.gitignore` 中忽略 `reference/`（或子路径），但物理位置仍须在工作区内。  

##  assistent 自检

每次写文件前确认：`path` 解析后仍位于 `D:\Workspaces\Github\HuangDiTujian` 之下。  

# 本地参考包（工作区内）

大体积史书文本、CHGIS zip、自用摘录等放在此目录下，例如：

```text
reference/
  texts/          # 公版原文克隆等
  gis/            # CHGIS 等开放数据
  notes/          # 私人笔记
```

- 默认被 `.gitignore` 忽略，不进 Git  
- **必须** 在仓库根下，不得写到工作区外  

规则见 `docs/06-工作区边界.md`。
